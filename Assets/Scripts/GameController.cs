using UnityEngine;
using System.Collections;

public class GameController : MonoBehaviour {
	
	//Sprites on game
	public Transform cat;
	public Transform mouse;
	public GameObject gouda;
	public GameObject brie;

	  

	// Use this for initialization
	void Start () {



		}


	
	// Update is called once per frame
	void Update () {

	/*
		//Detect if left button is clicked and if player can put more cheese.
		if(Input.GetButtonDown("Fire1") && cheeseSpent < cheeseStack){
			ray = Camera.main.ScreenPointToRay(Input.mousePosition);
			//putCheese = new Vector3(ray.,Input.mousePosition.y,1);
			Instantiate(cheese0, ray.origin, Quaternion.identity);
			cheeseSpent++;
		}
	*/


	}



}
